## Packages
date-fns | Date formatting for dashboard and visit lists
react-day-picker | Calendar component for date picking
recharts | Charts for the admin dashboard
framer-motion | Smooth animations for page transitions and UI elements

## Notes
- Auth uses cookie-based sessions (credentials: "include")
- File uploads use /api/upload endpoint (FormData)
- Geolocation API required for "Capture Location" feature
- Admin/Executive role separation handling in UI
